{
  "$schema": "https://langgra.ph/schema.json",
  "dependencies": ["."],
  "graphs": {
    "agent": "./src/agent/graph.py:graph",
    "router_agent": "./src/agent/router_graph.py:graph"
  },
  "env": ".env",
  "image_distro": "wolfi",
  "dockerfile_lines": [
    "RUN pip install uv && ln -s $(which uv) /usr/local/bin/uv && ln -s $(which uvx) /usr/local/bin/uvx"
]
}
